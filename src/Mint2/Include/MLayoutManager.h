#ifndef MLAYOUTMANAGER_H
#define MLAYOUTMANAGER_H

enum MLayoutType{
	MLT_HORIZONTAL = 0,
	MLT_VERTICAL,
};
/*
class MLayoutManager{
public:
	MLayoutManager(MLayoutType t);
	void Split(float t, MLayoutType t);
	void Split(int t, MLayoutType t);
	MLayerManager* GetLayerManager(int i);
	void Add(MLayerManer* pLM);
	void Assign(MWidget* pWidget, float x, float y);
	void Assign(MWidget* pWidget, int x, int y);
};
*/

#endif